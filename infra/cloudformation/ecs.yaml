AWSTemplateFormatVersion: '2010-09-09'
Description: ECS stack for coal-chatbot

Parameters:
  PrivateSubnet1:
    Type: String
  PrivateSubnet2:
    Type: String
  ECSSecurityGroup:
    Type: String
  TargetGroupArn:
    Type: String

Resources:
  ECSCluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: coal-chatbot-cluster

  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: coal-chatbot-task
      Cpu: "256"
      Memory: "512"
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      ExecutionRoleArn:
          Fn::ImportValue: coal-chatbot-execution-role
      ContainerDefinitions:
        - Name: coal-chatbot
          Image: coal-chatbot:latest
          Essential: true
          PortMappings:
            - ContainerPort: 5000

  ECSService:
    Type: AWS::ECS::Service
    DependsOn: ECSCluster
    Properties:
      Cluster: !Ref ECSCluster
      DesiredCount: 1
      LaunchType: FARGATE
      TaskDefinition: !Ref TaskDefinition
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnet1
            - !Ref PrivateSubnet2
      LoadBalancers:
        - ContainerName: coal-chatbot
          ContainerPort: 5000
          TargetGroupArn: !Ref TargetGroupArn